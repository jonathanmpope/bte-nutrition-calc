Your Results

# Goal: <%= @user.goal.titleize %>

<% @user.phases.each do |phase| %>
    <% if phase.name == 'performance' || phase.name == 'keto' || phase.name == 'fat loss' || phase.name == 'muscle gain' %>
        <%= phase.name.titleize %> Phase Macros can be viewed at this link: <%= "https://bte-nutrition-calculator.herokuapp.com/#{@user.id}/results/#{phase.id}" %>
    <% elsif phase.name == 'Intermittent fasting - fasting days' %>
        IF: Fasting Days Macros can be viewed at this link: <%= "https://bte-nutrition-calculator.herokuapp.com/#{@user.id}/results/#{phase.id}" %>
    <% else %>
        IF: Non-Fasting Days Macros can be viewed at this link": <%= "https://bte-nutrition-calculator.herokuapp.com/#{@user.id}/results/#{phase.id}" %>
    <% end %>
<% end %>